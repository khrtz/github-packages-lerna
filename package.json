{
  "name": "root",
  "version": "1.0.4",
  "main": "index.js",
  "private": true,
  "author": "khrtz <r.sqrrnbn9909@gmail.com>",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "publish": "lerna publish --no-verify-access",
    "publish:ci": "lerna publish --no-verify-access -y --canary --preid ci --dist-tag=ci --force-publish=*"
  },
  "dependencies": {
    "@khrtz/joker": "1.0.9"
  }
}
